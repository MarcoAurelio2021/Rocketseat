// Object
// criando um objeto const person ={aqui dentro colocamos propriedade e valores} 
const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true
}  
// console.log(person) // vai imprimir todo o meu objeto

// console.log(person.name) // para pegar somente a propriedade que quero eu uso um " ponto + a propriedade" EX: .name, .age, .weight

// usando a interpolação

console.log(`${person.name} tem ${person.age}`) 